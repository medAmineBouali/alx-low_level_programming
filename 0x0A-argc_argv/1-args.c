#include <stdio.h>
#include <stdlib.h>

/**
 * * main - Entry point
 * * Description:'the fun's description
 * * Return: (Success)
 */
int main(int argc,char* argv[])
{
	printf("%s\n", argv[0]);
	return (o);
}

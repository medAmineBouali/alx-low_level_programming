#include <stdio.h>
#include <stdlib.h>

/**
 * * main - Entry point
 * * Description:'the fun's description
 * * Return: (Success)
 */
int main(int argc,char* argv[] __attribute__ ((unused)))
{
	printf("%d\n", argc);
	return (o);
}
